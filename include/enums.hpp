#pragma once

namespace game {
    enum TileType {
        Empty = 0,
        Wall = 1,
        Spawn = 2,
        Base = 3,
        Turret = 4
    };

    enum TurretType{
        Gatling = 0
    };
};
